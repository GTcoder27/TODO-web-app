<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class=" flex bg-zinc-900 p-10 items-left">
        <a href="/logout" class="rounded-md p-2 inline-block bg-red-500 text-black-400">Logout</a>
    </div>
    <div class="w-full min-h-screen bg-zinc-900 p-2 text-white flex flex-col items-center">
        <h5 class="text-center">Edit Post.</h5>
        <form method="post" action="/update/<%= post._id%>" class=" items-center">
            <textarea class="resize-none bg-transparent border-2 border-zinc-600 rounded-md p-5" placeholder="what's in your mind" name="content" ><%= post.content%></textarea>
            <input class="px-3 py-2 w-55 bg-yellow-300 rounded-md text-black " type="submit" value="Update Post">
        </form>
        
    </div>
    
</body>
</html>